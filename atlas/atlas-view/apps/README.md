# Atlas Apps

`web/` is the canonical LUPI browser app and public viewer surface.

`remotion-trailer/` is a media/rendering support app. Do not create or route
work to a parallel `lupi-studio` app; that retired duplicate was removed because
it confused agents and duplicated the real viewer workflow.
