# Gallery Generators

These scripts regenerate bundled demo molecules, archive trajectories, and
gallery research fixtures for the LUPI web app.

Run them from anywhere; paths resolve back to `atlas-view` and write into the
intended `apps/web/public/...` asset folders. Do not place generator scripts in
`apps/web/public`, because everything in that folder is served by the app.
