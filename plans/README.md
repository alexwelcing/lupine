# Implementation Plans

Generated by the improve skill on 2026-06-10 (invocation: `plan` variant —
replace the public marketing page; audit skipped by design). Execute in the
order below. Each executor: read the plan fully before starting, honor its
STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Rebuild public marketing page as editorial prospectus; delete retired landing; guard deck surfaces | P1 | M | — | DONE (executed 2026-06-10, commit `7ee15b911` on branch `worktree-agent-a10a40251941ed79b`; reviewed and approved — all done criteria re-verified. Awaiting user merge.) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- None — single plan.

## Findings considered and rejected

- **Rewriting the narrative itself**: the "Step 1 of a real-world Replicator"
  floor+ceiling frame is a deliberately adopted decision (landed via PR #219);
  this plan replaces the *design and factual defects*, not the narrative.
- **Restyling `access.html` / `one-pager.html` / `deck.html`**: deferred —
  separate surfaces, and the one-pager is print-targeted; keep this change
  reviewable.
- **Touching `deck/server.mjs`**: not needed — the serving contract (`/` →
  `index.html`, gated `/deck.html`, `/health`) is correct and tested.
