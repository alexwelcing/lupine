{
  "name": "library-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Lupine Science research library — mobile-first reader for library.lupine.science",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && node scripts/serve.js",
    "clean": "rm -rf dist",
    "content:export": "node ../scripts/export_library_content.mjs"
  },
  "dependencies": {
    "force-graph": "^1.51.4",
    "katex": "^0.17.0",
    "marked": "^12.0.2"
  }
}
