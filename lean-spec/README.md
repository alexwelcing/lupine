# Lean Spec

This directory contains formal definitions used by the promotion pipeline.

Current MVP includes:
- FCC elasticity definitions (`K`, `G`, `A`)
- A simple linear operator form (`predictShearError`)
- A baseline validity class (`mvpFccMetals`)
