# Formalize Profile Memory

- Every promoted claim requires theorem targets and explicit assumptions.
- Validity classes must be materialized in Lean scope objects.
- Proof status is synced into OperatorPack metadata.
