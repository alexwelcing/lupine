# Distill Profile Memory

- Rust is the canonical transform layer.
- Python generates candidate operators from error vectors.
- Counterexamples are first-class artifacts and must be emitted explicitly.
