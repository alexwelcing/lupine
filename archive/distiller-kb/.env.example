# Open Distillation Factory — secrets template
# Copy to .env and fill in real values. .env is gitignored.

# MiniMax Token Plan (provider A — primary, high-volume text + modal quotas)
MINIMAX_TOKEN_KEY=
MINIMAX_API_KEY=          # Hermes Agent expects this name; set to same value as MINIMAX_TOKEN_KEY

# Z.ai coding pro plan (provider B — fallback + deep reasoning, GLM family)
ZAI_TOKEN_KEY=
GLM_API_KEY=              # Hermes Agent expects this name; set to same value as ZAI_TOKEN_KEY
